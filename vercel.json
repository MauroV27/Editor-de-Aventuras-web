{
    "version": 2,
    "builds": [
      {
        "src": "**/*.js",
        "use": "@vercel/node",
        "config": {
          "runtime": "nodejs20.x"
        }
      }
    ],
    "ignore": ["tests"],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/server.js"
      }
    ]
  }